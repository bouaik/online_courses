- content_for :title do 
  OnlineCourses101
  \|
  Edit 
  = @course.title

.card
  .card-header
    %h3
      .fa.fa-graduation-cap
      Editing course
  .card-body
    = render 'form'
  .card-footer
    = link_to 'Back', courses_path
